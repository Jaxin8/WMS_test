#简要说明

##你使用了哪些 AI 工具？如何使用的？
1、工具选型与切换：
本打算使用 Trae 这个 IDE 来做，它内置了 AI，且对代码能够深度理解、智能补全与生成代码、Bug 自动修复等。但是近期免费的内置 AI 排队比较久，影响开发节奏。
所以就改用 IntelliJ IDEA，并安装了 通义灵码 (Lingma) 插件。

（2）交互方式：
侧边栏对话：在右边窗口直接询问架构设计、技术选型或报错原因（例如分析 Git 推送失败的具体原因）。
代码选中解释/优化：在编辑器中选中特定代码块（如 Service 层的事务逻辑），要求 AI 分析潜在问题（如并发安全、N+1 性能问题）并提供重构方案。
上下文感知：AI 能读取当前项目结构，根据已有的 Entity 和 DTO 自动生成对应的 Controller 接口和 Swagger 文档注解。

（3）具体应用场景：
代码重构：将冗长的 `createInboundOrder` 方法拆分为职责单一的私有方法，并引入批量处理（`saveAll`）解决 N+1 查询问题。
环境排错：快速定位 Windows 环境下 Git SSH 连接被重定向到本地回环地址（::1）的配置错误，以及 HTTPS 推送时的内存溢出问题。
规范落地：根据 API 规范自动生成符合 RESTful 风格的 Controller，并集成 SpringDoc OpenAPI 实现接口文档自动化。

##遇到了什么问题？如何解决的？
### 1. Git 推送失败与内存溢出问题
问题：在将代码推送到 GitHub 时，频繁遇到 `fatal: Out of memory, malloc failed` 或 `Connection closed by ::1 port 22` 错误。

原因分析：
Windows 系统虚拟内存（页面文件）设置不足，导致 Git 进程在处理打包压缩时崩溃。
SSH 配置文件（`.ssh/config`）存在错误的代理设置，将 `github.com` 解析到了本地回环地址。
HTTPS 推送时，IDEA 的认证脚本因内存不足无法弹出输入框。

解决方案：
调整系统配置：增加 Windows 虚拟内存大小，并重启电脑释放资源。
修正 Git 配置：清理错误的 SSH 代理配置，切换为更稳定的 SSH 协议推送。
优化 Git 参数：使用 `git config --global pack.threads 1` 降低打包时的内存压力。

### 2. 事务逻辑中的并发安全与性能瓶颈
问题描述：在实现入库单创建功能时，AI生成原始的循环处理方式存在 N+1 查询性能问题，且库存更新逻辑在高并发下可能导致数据不一致。

原因分析：
逐条保存明细导致频繁的数据库交互。
“读取-修改-写入”模式在多线程环境下会产生竞态条件（Lost Update）。

解决方案：
批量处理：使用 JPA 的 `saveAll` 方法配合 Stream 流，将 N 次插入合并为一次 JDBC Batch 操作。
原子更新：在 Repository 层编写自定义 JPQL (`UPDATE ... SET quantity = quantity + ?`)，利用数据库层面的原子性保证库存准确。



##如果有更多时间，你还会做什么？
1、完善入库、出库的业务闭环与交互体验，目前入库单创建后直接进入 `PENDING` 状态，且前端交互较为简单。
2、增加“入库记录查询”功能，支持按时间段、供应商导出报表等。
3、在前端引入的弹窗表单，支持动态增删明细行，并增加实时库存预校验（即在填写表单时提示该库位当前剩余容量）。
4、引入工作流引擎实现审批机制，目前对数据的修改（如删除商品、调整库存）是直接生效的，缺乏风控。只有当流程审批通过后，才触发实际的库存变更逻辑，确保业务操作的合规性。
5、更新库存、发送通知不是核心逻辑，可以引入消息队列做成异步，进一步降低入库接口的响应延迟。
6、加入日志与监控


